name: pr-issue-linkage

# Standalone required check that validates the PR body carries a native closing
# keyword and a non-empty Related section. It runs on pull_request_target so the
# base-branch definition gates and a head-branch edit cannot bypass it; it reads
# body metadata from the event payload only and checks out no head code. The
# emitted required-check context is `pr-issue-linkage / pr-issue-linkage`.
on:
  pull_request_target:
    types: [opened, edited, reopened, synchronize]
  merge_group:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  select-runner:
    permissions: {}
    uses: melodic-software/ci-workflows/.github/workflows/select-runner.yml@90f1c54cf871a3aed4f1eb14a279a1651975a794 # 90f1c54 2026-07-18
    secrets:
      observer-private-key: ${{ secrets.CI_RUNNER_OBSERVER_PRIVATE_KEY }}
    with:
      policy: ${{ vars.CI_RUNNER_POLICY }}
      self-hosted-label: ${{ vars.CI_SELF_HOSTED_LABEL }}
      hosted-runner: ${{ vars.CI_HOSTED_RUNNER }}
      scope: ${{ vars.CI_RUNNER_SCOPE }}
      managed-runner-prefix: ${{ vars.CI_MANAGED_RUNNER_PREFIX }}
      observer-client-id: ${{ vars.CI_RUNNER_OBSERVER_CLIENT_ID }}

  pr-issue-linkage:
    needs: select-runner
    if: ${{ always() }}
    permissions: {}
    uses: melodic-software/ci-workflows/.github/workflows/pr-issue-linkage.yml@90f1c54cf871a3aed4f1eb14a279a1651975a794 # 90f1c54 2026-07-18
    with:
      runner: ${{ needs.select-runner.outputs.runner || 'ubuntu-24.04' }}
      prerequisite-result: ${{ needs.select-runner.result }}
