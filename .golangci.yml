version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - nolintlint
    - staticcheck
    - unused
  settings:
    nolintlint:
      allow-unused: false
      require-explanation: true
      require-specific: true
